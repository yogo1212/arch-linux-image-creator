#!/bin/sh -e

# $1 kernel
initramfs="$2"
# $3 UKI (opt.)
# KERNELVERSION KERNELDESTINATION

# TODO if this can be made to work, might want to ditch the libalpm hook

echo "$initramfs" | /usr/bin/alarm-efi-update files

cat >/tmp/asdf <<EOF
$1
$2
$3
EOF

env >>/tmp/asdf
